# Helper files and local test files which should not be committed.
.last_modified
test_working_dir/
tests/output/*.result.txt
prm/projects/.*.tmp

# External sources which shouldn't be committed.
src/
